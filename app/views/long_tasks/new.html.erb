<h1>Nueva Tarea Larga</h1>

<%= simple_form_for(@long_task, url: { :action => :create }, remote: true)  do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :description, label: 'Descripción' %>
    <%= f.input :state, collection: long_states, label: "Estado" %>
    <%= f.input :priority, collection: priorities, label: "Prioridad" %>
    <%= f.input :progress, label: "Progreso" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, 'Crear Tarea Larga' %>
  </div>

<% end %>
<br>
<%= link_to 'Volver', list_path(@long_task.list), class: "btn btn-info" %>


