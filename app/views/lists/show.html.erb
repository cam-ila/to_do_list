<div class="container">  
  <table class="table table-hover" >


<p>
  <strong>Name:</strong>
  <%= @list.name %>

   <strong>Url:</strong>
  <%= @list.url %>
	</p>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
 

</table>
</div>